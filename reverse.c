#include <stdio.h>
int main(){
    printf("Enter the no.of integers:-");
    int n;
    scanf("%d",&n);
    printf("Enter the integers:-");
    for (int i=0;i<n;i++)
    {
        scanf("%d ",&a[i]);
    }
    
}